spring:
    application:
        name: library-design-pattern

domain-config:
    database-connection-config:
        driver: h2
        protocol: mem
        database: testdb
        options:
            DB_CLOSE_DELAY: "-1"
